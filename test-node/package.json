{
  "name": "test-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -p tsconfig.json",
    "copy": "cp -r ../secret/ ./lib/test_node/src",
    "build": "npm run compile && npm run copy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "http": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^13.5.1",
    "typescript": "^3.7.5"
  }
}
